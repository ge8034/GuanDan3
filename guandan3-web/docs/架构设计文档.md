# 掼蛋游戏架构设计文档

## 目录

- [概述](#概述)
- [系统架构](#系统架构)
- [技术栈](#技术栈)
- [前端架构](#前端架构)
- [后端架构](#后端架构)
- [数据流设计](#数据流设计)
- [状态管理](#状态管理)
- [实时通信](#实时通信)
- [安全设计](#安全设计)
- [性能优化](#性能优化)
- [部署架构](#部署架构)

## 概述

本文档描述了掼蛋游戏的系统架构设计，包括前端、后端、数据库、实时通信等各个层面的技术实现和设计决策。

### 项目目标

- **高性能**：支持多人实时对战，低延迟响应
- **可扩展**：模块化设计，易于功能扩展
- **安全性**：数据安全，用户隐私保护
- **用户体验**：流畅的动画，直观的界面
- **跨平台**：支持桌面和移动设备

### 设计原则

1. **前后端分离**：清晰的职责划分
2. **实时优先**：基于WebSocket的实时通信
3. **组件化**：可复用的UI组件
4. **类型安全**：TypeScript类型检查
5. **性能优化**：代码分割、懒加载、缓存策略

## 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Web浏览器   │  │   移动设备    │  │   桌面应用    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      前端应用层                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Next.js    │  │   React 19   │  │  TypeScript  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  Zustand     │  │  Framer Motion│  │  Tailwind CSS│ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      API网关层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  Supabase    │  │  REST API    │  │  WebSocket   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      业务逻辑层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  游戏引擎    │  │  用户管理    │  │  房间管理    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  聊天系统    │  │  好友系统    │  │  统计系统    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ PostgreSQL   │  │  Redis缓存   │  │  文件存储    │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 分层架构

#### 1. 表现层（Presentation Layer）
- **职责**：用户界面展示和交互
- **技术**：React 19、Next.js 16、Tailwind CSS
- **组件**：页面组件、UI组件、业务组件

#### 2. 业务逻辑层（Business Logic Layer）
- **职责**：游戏规则、业务逻辑处理
- **技术**：TypeScript、自定义游戏引擎
- **模块**：游戏引擎、AI系统、规则验证

#### 3. 数据访问层（Data Access Layer）
- **职责**：数据持久化和检索
- **技术**：Supabase、PostgreSQL
- **功能**：CRUD操作、查询优化、事务管理

#### 4. 基础设施层（Infrastructure Layer）
- **职责**：底层服务和工具
- **技术**：WebSocket、实时订阅、缓存
- **服务**：认证、授权、日志、监控

## 技术栈

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 16 | React框架，SSR/SSG支持 |
| React | 19 | UI库 |
| TypeScript | 5.x | 类型安全 |
| Tailwind CSS | 4.x | 样式框架 |
| Framer Motion | 11.x | 动画库 |
| Zustand | 4.x | 状态管理 |
| Supabase JS | 2.x | 后端服务客户端 |
| Three.js | r128 | 3D渲染 |
| React Three Fiber | 8.x | React 3D绑定 |

### 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Supabase | Latest | BaaS平台 |
| PostgreSQL | 15+ | 关系型数据库 |
| Supabase Realtime | Latest | 实时通信 |
| Supabase Auth | Latest | 用户认证 |
| Supabase Storage | Latest | 文件存储 |

### 开发工具

| 工具 | 版本 | 用途 |
|------|------|------|
| Vitest | Latest | 单元测试 |
| Playwright | Latest | E2E测试 |
| ESLint | Latest | 代码检查 |
| Prettier | Latest | 代码格式化 |
| TypeScript | 5.x | 类型检查 |

## 前端架构

### 目录结构

```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 认证相关页面
│   ├── (game)/            # 游戏相关页面
│   ├── (social)/          # 社交相关页面
│   ├── api/               # API路由
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 首页
├── components/            # React组件
│   ├── ui/               # 基础UI组件
│   ├── game/             # 游戏相关组件
│   ├── social/           # 社交相关组件
│   └── layout/           # 布局组件
├── lib/                 # 工具库
│   ├── supabase/        # Supabase客户端
│   ├── store/           # Zustand状态管理
│   ├── api/             # API封装
│   ├── services/        # 业务服务
│   ├── utils/           # 工具函数
│   └── hooks/          # 自定义Hooks
├── types/              # TypeScript类型定义
├── styles/             # 全局样式
└── test/              # 测试文件
```

### 组件架构

#### 组件层次结构

```
App
├── Layout
│   ├── Header
│   ├── Main
│   └── Footer
├── Pages
│   ├── HomePage
│   ├── LobbyPage
│   ├── RoomPage
│   └── StatsPage
└── Components
    ├── UI Components
    │   ├── Button
    │   ├── Card
    │   ├── Input
    │   └── Modal
    ├── Game Components
    │   ├── GameBoard
    │   ├── PlayerHand
    │   ├── GameControls
    │   └── ScoreBoard
    └── Social Components
        ├── ChatBox
        ├── FriendList
        └── UserProfile
```

#### 组件设计原则

1. **单一职责**：每个组件只负责一个功能
2. **可复用性**：组件设计为可复用
3. **性能优化**：使用React.memo、useMemo、useCallback
4. **类型安全**：完整的TypeScript类型定义
5. **可测试性**：组件易于单元测试

### 状态管理架构

#### Zustand Store设计

```typescript
// 游戏状态管理
interface GameStore {
  // 状态
  gameId: string | null
  status: GameStatus
  turnNo: number
  currentSeat: number
  myHand: Card[]
  lastAction: Turn | null
  
  // 操作
  startGame: (roomId: string) => Promise<void>
  submitTurn: (action: GameAction) => Promise<void>
  resetGame: () => void
}

// 房间状态管理
interface RoomStore {
  // 状态
  currentRoom: Room | null
  members: RoomMember[]
  
  // 操作
  createRoom: (config: RoomConfig) => Promise<Room>
  joinRoom: (roomId: string, seatNo: number) => Promise<void>
  leaveRoom: (roomId: string) => Promise<void>
}

// 用户状态管理
interface UserStore {
  // 状态
  user: User | null
  profile: Profile | null
  
  // 操作
  login: () => Promise<void>
  logout: () => Promise<void>
  updateProfile: (data: Partial<Profile>) => Promise<void>
}
```

#### 状态更新流程

```
用户操作 → 组件事件 → Store Action → 状态更新 → 组件重渲染
```

### 路由架构

#### Next.js App Router

```typescript
// 路由结构
app/
├── (auth)/
│   ├── login/
│   │   └── page.tsx
│   └── register/
│       └── page.tsx
├── (game)/
│   ├── lobby/
│   │   └── page.tsx
│   ├── room/
│   │   └── [roomId]/
│   │       └── page.tsx
│   └── stats/
│       └── page.tsx
└── (social)/
    ├── friends/
    │   └── page.tsx
    └── chat/
        └── page.tsx
```

#### 路由守卫

```typescript
// 中间件保护
export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl
  
  // 检查认证状态
  const isAuthenticated = checkAuth(request)
  
  // 重定向未认证用户
  if (!isAuthenticated && pathname.startsWith('/game')) {
    return NextResponse.redirect(new URL('/login', request.url))
  }
  
  return NextResponse.next()
}
```

## 后端架构

### Supabase架构

#### 服务组件

```
Supabase Platform
├── PostgreSQL Database
│   ├── Tables
│   ├── Views
│   ├── Functions
│   └── Triggers
├── Authentication
│   ├── User Management
│   ├── Session Management
│   └── OAuth Integration
├── Realtime
│   ├── WebSocket Server
│   ├── Subscription Management
│   └── Broadcast System
├── Storage
│   ├── File Upload
│   ├── CDN Distribution
│   └── Image Transformation
└── Edge Functions
    ├── API Endpoints
    ├── Webhooks
    └── Custom Logic
```

### 数据库架构

#### 表设计原则

1. **规范化**：遵循第三范式
2. **索引优化**：基于查询模式创建索引
3. **分区策略**：大表按时间分区
4. **JSONB使用**：灵活存储复杂数据

#### 存储过程设计

```sql
-- 游戏逻辑存储过程
CREATE OR REPLACE FUNCTION submit_turn(
  p_game_id uuid,
  p_action_id uuid,
  p_expected_turn_no int,
  p_payload jsonb
)
RETURNS TABLE (
  turn_no int,
  current_seat int,
  status text,
  rankings int[]
) AS $$
BEGIN
  -- 验证回合号
  -- 插入回合记录
  -- 更新游戏状态
  -- 返回新状态
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

### API设计

#### RESTful API

```typescript
// API端点设计
const apiEndpoints = {
  // 用户管理
  'GET /api/users/:id': '获取用户信息',
  'PUT /api/users/:id': '更新用户信息',
  
  // 房间管理
  'POST /api/rooms': '创建房间',
  'GET /api/rooms': '获取房间列表',
  'POST /api/rooms/:id/join': '加入房间',
  'DELETE /api/rooms/:id': '离开房间',
  
  // 游戏管理
  'POST /api/games/:id/start': '开始游戏',
  'POST /api/games/:id/turn': '提交回合',
  'POST /api/games/:id/pause': '暂停游戏',
  'POST /api/games/:id/resume': '恢复游戏',
  
  // 社交功能
  'POST /api/friends': '添加好友',
  'GET /api/friends': '获取好友列表',
  'POST /api/chat': '发送消息',
  'GET /api/chat/:roomId': '获取聊天记录'
}
```

#### RPC调用

```typescript
// Supabase RPC调用
const { data, error } = await supabase.rpc('submit_turn', {
  p_game_id: gameId,
  p_action_id: actionId,
  p_expected_turn_no: expectedTurnNo,
  p_payload: payload
})
```

## 数据流设计

### 游戏数据流

```
用户操作
  ↓
前端验证
  ↓
提交到服务器
  ↓
服务器验证
  ↓
更新数据库
  ↓
触发实时更新
  ↓
客户端接收更新
  ↓
更新UI
```

### 实时数据流

```
数据库变更
  ↓
PostgreSQL触发器
  ↓
Supabase Realtime
  ↓
WebSocket推送
  ↓
客户端订阅
  ↓
状态更新
  ↓
UI重渲染
```

### 状态同步流程

```
1. 客户端发起操作
2. 前端乐观更新（可选）
3. 发送到服务器
4. 服务器处理并验证
5. 更新数据库
6. 广播变更给所有订阅者
7. 客户端接收并更新状态
8. 处理冲突（如有）
```

## 实时通信

### WebSocket连接

```typescript
// 建立WebSocket连接
const channel = supabase
  .channel('game-updates')
  .on('postgres_changes', {
    event: '*',
    schema: 'public',
    table: 'games'
  }, (payload) => {
    // 处理游戏状态更新
    handleGameUpdate(payload)
  })
  .subscribe((status) => {
    if (status === 'SUBSCRIBED') {
      console.log('已订阅游戏更新')
    }
  })
```

### 订阅管理

```typescript
// 订阅管理器
class SubscriptionManager {
  private subscriptions: Map<string, RealtimeChannel> = new Map()
  
  subscribe(channelName: string, callback: Function) {
    const channel = supabase.channel(channelName)
      .on('postgres_changes', callback)
      .subscribe()
    
    this.subscriptions.set(channelName, channel)
    return channel
  }
  
  unsubscribe(channelName: string) {
    const channel = this.subscriptions.get(channelName)
    if (channel) {
      channel.unsubscribe()
      this.subscriptions.delete(channelName)
    }
  }
  
  unsubscribeAll() {
    this.subscriptions.forEach(channel => channel.unsubscribe())
    this.subscriptions.clear()
  }
}
```

### 断线重连

```typescript
// 断线重连逻辑
const handleReconnect = async () => {
  let retryCount = 0
  const maxRetries = 5
  
  while (retryCount < maxRetries) {
    try {
      await reconnect()
      console.log('重连成功')
      break
    } catch (error) {
      retryCount++
      const delay = Math.pow(2, retryCount) * 1000
      await sleep(delay)
    }
  }
}
```

## 安全设计

### 认证机制

```typescript
// 用户认证流程
const authenticate = async () => {
  // 1. 检查本地会话
  const { data: { session } } = await supabase.auth.getSession()
  
  if (session) {
    return session.user
  }
  
  // 2. 匿名登录
  const { data, error } = await supabase.auth.signInAnonymously()
  
  if (error) {
    throw new Error('认证失败')
  }
  
  return data.user
}
```

### 授权机制

```sql
-- Row Level Security策略
CREATE POLICY "Users can view their own data"
ON profiles
FOR SELECT
USING (auth.uid() = uid);

CREATE POLICY "Users can update their own data"
ON profiles
FOR UPDATE
USING (auth.uid() = uid);
```

### 数据加密

```typescript
// 敏感数据加密
const encryptData = (data: string, key: string): string => {
  const encrypted = CryptoJS.AES.encrypt(data, key).toString()
  return encrypted
}

const decryptData = (encrypted: string, key: string): string => {
  const decrypted = CryptoJS.AES.decrypt(encrypted, key)
  return decrypted.toString(CryptoJS.enc.Utf8)
}
```

### 输入验证

```typescript
// 输入验证
const validateInput = (input: any, schema: z.Schema) => {
  try {
    schema.parse(input)
    return { valid: true, errors: null }
  } catch (error) {
    if (error instanceof z.ZodError) {
      return { valid: false, errors: error.errors }
    }
    return { valid: false, errors: ['验证失败'] }
  }
}
```

## 性能优化

### 前端优化

#### 1. 代码分割

```typescript
// 动态导入
const GameBoard = dynamic(() => import('@/components/game/GameBoard'), {
  loading: () => <LoadingSpinner />
})

const StatsPage = dynamic(() => import('@/app/stats/page'))
```

#### 2. 图片优化

```typescript
// Next.js Image组件
import Image from 'next/image'

<Image
  src="/avatar.jpg"
  alt="用户头像"
  width={100}
  height={100}
  priority={false}
  loading="lazy"
/>
```

#### 3. 缓存策略

```typescript
// React Query缓存
const { data, isLoading } = useQuery({
  queryKey: ['user', userId],
  queryFn: () => fetchUser(userId),
  staleTime: 5 * 60 * 1000, // 5分钟
  cacheTime: 10 * 60 * 1000 // 10分钟
})
```

### 后端优化

#### 1. 数据库索引

```sql
-- 创建复合索引
CREATE INDEX idx_games_room_status 
ON games(room_id, status);

CREATE INDEX idx_turns_game_created 
ON turns(game_id, created_at DESC);
```

#### 2. 查询优化

```sql
-- 使用CTE优化复杂查询
WITH ranked_games AS (
  SELECT 
    id,
    room_id,
    status,
    ROW_NUMBER() OVER (PARTITION BY room_id ORDER BY created_at DESC) as rn
  FROM games
  WHERE status IN ('playing', 'paused')
)
SELECT * FROM ranked_games WHERE rn = 1;
```

#### 3. 连接池

```typescript
// Supabase连接池配置
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
  {
    db: {
      schema: 'public'
    },
    global: {
      headers: {
        'Connection': 'keep-alive'
      }
    }
  }
)
```

### 网络优化

#### 1. 请求合并

```typescript
// 批量请求
const batchRequests = async (requests: Request[]) => {
  const responses = await Promise.all(
    requests.map(req => fetch(req.url, req.options))
  )
  return responses
}
```

#### 2. 数据压缩

```typescript
// 启用压缩
const compressData = (data: any): string => {
  const json = JSON.stringify(data)
  return pako.deflate(json, { to: 'string' })
}

const decompressData = (compressed: string): any => {
  const json = pako.inflate(compressed, { to: 'string' })
  return JSON.parse(json)
}
```

## 部署架构

### 部署环境

```
┌─────────────────────────────────────────────────────────────┐
│                      生产环境                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Vercel     │  │  Supabase    │  │   CDN        │ │
│  │  (前端)      │  │  (后端)      │  │  (静态资源)  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      开发环境                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Localhost  │  │  Supabase    │  │   Mock       │ │
│  │  (前端)      │  │  (开发)      │  │  (测试)      │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### CI/CD流程

```
代码提交
  ↓
GitHub Actions触发
  ↓
运行测试
  ↓
构建应用
  ↓
部署到Vercel
  ↓
运行E2E测试
  ↓
部署完成
```

### 监控和日志

```typescript
// 错误监控
const logError = (error: Error, context: any) => {
  console.error('Error:', error)
  
  // 发送到监控服务
  if (typeof window !== 'undefined') {
    window.gtag?.('event', 'exception', {
      description: error.message,
      fatal: false
    })
  }
}

// 性能监控
const measurePerformance = (name: string, fn: Function) => {
  const start = performance.now()
  const result = fn()
  const end = performance.now()
  
  console.log(`${name} took ${end - start}ms`)
  return result
}
```

## 附录

### 技术决策记录

| 决策 | 原因 | 影响 |
|------|------|------|
| 使用Next.js 16 | 最新特性，SSR支持 | 更好的SEO和性能 |
| 使用Supabase | 快速开发，内置功能 | 减少后端开发时间 |
| 使用Zustand | 轻量级，简单易用 | 更好的开发体验 |
| 使用TypeScript | 类型安全 | 减少运行时错误 |
| 使用Tailwind CSS | 快速开发，一致性 | 更快的UI开发 |

### 性能指标

| 指标 | 目标值 | 当前值 |
|------|--------|--------|
| 首屏加载时间 | < 2s | 1.5s |
| 交互响应时间 | < 100ms | 80ms |
| 游戏帧率 | > 30fps | 45fps |
| 内存使用 | < 200MB | 150MB |
| 网络延迟 | < 100ms | 50ms |

### 扩展性考虑

1. **水平扩展**：支持多实例部署
2. **数据库分片**：大表按用户ID分片
3. **缓存层**：Redis缓存热点数据
4. **CDN加速**：静态资源CDN分发
5. **负载均衡**：多服务器负载均衡

---

**文档维护者**：开发团队  
**最后更新**：2026-03-21  
**联系方式**：support@example.com
