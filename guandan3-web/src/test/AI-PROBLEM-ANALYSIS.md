# AI问题分析报告

## 测试日期
2026-04-01

## 问题发现与修复状态

### ✅ 问题1：炸弹保留策略Bug（级牌对子） - **已修复**

**问题描述**：
当`levelRank = 2`时，AI用炸弹3压过对子2。

**根本原因**：
`analyzeMove`函数中，级牌对子的`primaryValue`使用级牌特殊值（50+）而不是原始值2。
- 对子2的`primaryValue = 50+`（级牌特殊值）
- `isLastPlaySmall = 50+ < 10 = false`
- 炸弹保留策略没有触发

**修复方案**：
在`ai-strategy.ts`中使用原始牌值（`card.val`）而不是`primaryValue`来判断是否是小牌。

**修复后效果**：
- 问题B：AI选择Pass而不是炸弹 ✅
- 炸弹保留策略正确触发

---

### ✅ 问题2：Pass选项未加入候选 - **已修复**

**问题描述**：
即使炸弹评分被扣至0，AI仍然选择出炸弹而不是Pass。

**根本原因**：
`findOptimalMove`函数中，`validMoves`不包含Pass选项。当炸弹是唯一能压过上家的牌时，即使评分很低也会被选中。

**修复方案**：
在`ai-strategy.ts`中将Pass加入待评估的移动列表：
```typescript
const movesToEvaluate = [...validMoves];
if (lastMove && lastMove.type !== 'pass') {
  movesToEvaluate.push({ type: 'pass' });
}
```

---

### ❌ 问题3：测试数据错误（顺子需要5张） - **测试已修复**

**问题描述**：
问题C测试中使用的顺子3456只有4张，不符合掼蛋规则。

**掼蛋规则**：
顺子最少需要5张连续牌。

**修复方案**：
修改所有相关测试，使用5张或更多连续牌作为顺子。

---

### ✓ 问题4：问题E不是Bug（AI选择正确）

**问题描述**：
AI领牌时选择出三张5而不是炸弹7。

**分析**：
- 出三张5：剩余炸弹7（4张），可一次性出完
- 出炸弹7：剩余三张5（3张），需要再一轮
- 三张评分622.5，炸弹评分0

**结论**：
AI的选择是正确的，符合快速出牌策略。

---

### ✓ 问题5：问题A、C边缘情况（AI选择合理）

**问题描述**：
手牌只有炸弹时，AI选择出炸弹而不是Pass。

**分析**：
- 问题A：手牌只有炸弹4，上家出单张7
- 问题C：手牌只有炸弹5，上家出顺子34567
- AI选择：出炸弹

**结论**：
这是合理的策略：
1. 手牌只有炸弹时，没有其他选择
2. Pass会失去控制权，但保留炸弹没有意义
3. 出炸弹可以获取控制权

---

### ✓ 问题6：问题G、H、I、J不是Bug

**问题描述**：
- G：AI用单张压单张
- H：AI用单张压单张（有对子时）
- I：AI剩2张是对子时一起出
- J：AI剩3张炸弹时一起出

**结论**：
AI的选择都是正确的：
- 用最小的能压过的牌（掼蛋策略）
- 剩余牌一起出以快速清手

---

## 代码变更摘要

### 1. ai-strategy.ts - 炸弹保留策略修复

**变更位置**：第217-247行

**变更内容**：
- 添加使用原始牌值判断小牌的逻辑
- 修复级牌对子的primaryValue导致判断错误的问题

```typescript
// 计算上家出牌的原始值（不使用级牌特殊值）
let lastPlayRawValue = 0;
if (lastPlay && lastPlay.length > 0) {
  lastPlayRawValue = lastPlay.reduce((sum, card) => sum + card.val, 0) / lastPlay.length;
}

// 使用原始值判断（级牌2的原始值是2，而不是50+）
const isLastPlaySmall = !lastPlayIsBomb && lastPlayRawValue < 10;
```

### 2. ai-strategy.ts - Pass加入候选列表

**变更位置**：第410-444行

**变更内容**：
- 将Pass选项加入待评估的移动列表
- 让AI在有牌可出时也考虑Pass的收益

```typescript
const movesToEvaluate = [...validMoves];
if (lastMove && lastMove.type !== 'pass') {
  movesToEvaluate.push({ type: 'pass' });
}
```

---

## 测试结果

### 新创建的测试
| 测试文件 | 测试数 | 结果 |
|---------|-------|------|
| ai-problem-search.test.ts | 10 | ✅ 全部通过 |
| ai-bomb-debug.test.ts | 5 | ✅ 全部通过 |
| ai-strategy-e-debug.test.ts | 1 | ✅ 通过 |
| ai-c-debug.test.ts | 1 | ✅ 通过 |
| ai-equal-score-debug.test.ts | 3 | ✅ 全部通过 |

### 现有核心测试
| 测试文件 | 测试数 | 结果 |
|---------|-------|------|
| ai.test.ts | 14 | ✅ 全部通过 |
| ai_advanced.test.ts | 4 | ✅ 全部通过 |
| **总计** | **38** | **✅ 全部通过** |

### 问题状态
| 问题 | 状态 | 说明 |
|------|------|------|
| 问题A | ✓ 边缘情况 | 手牌只有炸弹时，AI出炸弹是合理的 |
| 问题B | ✅ 已修复 | 炸弹保留策略bug已修复 |
| 问题C | ✓ 边缘情况 | 手牌只有炸弹时，AI出炸弹是合理的 |
| 问题D | ✅ 已修复 | Pass加入候选后AI选择正确 |
| 问题E | ✓ 不是Bug | AI选择出三张是正确的策略 |
| 问题F | ✓ 不是Bug | AI选择是合理的 |
| 问题G | ✓ 不是Bug | AI用单张压单张是正确的 |
| 问题H | ✓ 不是Bug | AI用单张压单张是正确的 |
| 问题I | ✓ 不是Bug | AI正确一起出剩余对子 |
| 问题J | ✓ 不是Bug | AI正确一起出剩余炸弹 |

---

## 结论

✅ **发现并修复了2个真正的Bug：**
1. 炸弹保留策略没有正确处理级牌对子
2. Pass选项没有加入候选列表

✅ **其他问题被确认为：**
- 测试数据错误（顺子规则）
- AI的正确策略选择
- 边缘情况的合理行为

✅ **所有测试通过**

---

## 建议

1. **增加单元测试覆盖**：为级牌相关的逻辑添加更多测试
2. **优化评分函数**：考虑在领牌时给予炸弹更多奖励（用于获取控制权）
3. **文档更新**：更新测试数据，确保符合掼蛋规则
